// ExpressJS
import { Router } from 'express';
// Express Validators
import { check } from 'express-validator';


const router = Router();

// TODO: PATH: /api/tags/create
router.post();
// TODO: PATH: /api/tags/
router.get();
// TODO: PATH: /api/tags/update/:id
router.put();
// TODO: PATH: /api/tags/delete/:id
router.delete();

export default router;
